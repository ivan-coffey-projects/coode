# Contributor notes

- Read `README.md` before changing the server, bridge, panel, or overlay.
- Keep credentials, local paths, generated output, and editor data out of Git.
- Run `pnpm test` and `pnpm build` before submitting changes.
- Keep the service bound to loopback unless authentication is added.
